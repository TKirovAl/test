#include <iostream>

 using namespace std;

 
int main () 
 
{
    if (x>0 && y>0) Q = 1;
    
    else if (x<0 && y>0) Q = 2;
    
    else if (x<0 && y<0) Q = 3;
    
    else if (x>0 && y<0) Q = 4;
    
    else Q = 0;
    
    printf("%d-я четверть\n", Q);
    
    return 0;
}
